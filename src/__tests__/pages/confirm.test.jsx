// !STARTERCONF You should delete this page

import { render, screen } from '@testing-library/react';

import Page from '@/app/confirm/page'
describe('Login', () => {
  it('renders the Page', () => {
    render(<Page />);

  });
});
